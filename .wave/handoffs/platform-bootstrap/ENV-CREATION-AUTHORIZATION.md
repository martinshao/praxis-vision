# 首次开发环境文件创建授权

用户原话：$sw bootstrap 允许官方生成器在首次初始化时创建开发用 `apps/web/.env`

授权仅覆盖官方生成器首次自动创建开发.env；不读取或展示其内容，不改生产配置，不授予其他环境文件编辑权限。该明确指令优先于AGENTS与bootstrap工作流的一般禁写要求，无需重复确认。原初始化组合、no-git/no-install、独立安装与验证不变。
