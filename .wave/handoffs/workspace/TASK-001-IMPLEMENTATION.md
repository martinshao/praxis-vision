# TASK-001 实施计划

1. database-engineer 串行交接：单例 Owner 与保留数据的幂等迁移，临时 SQLite 验证。
2. Better Auth 关闭注册，创建会话与可信身份只接受单例所有者；HTTP 端点白名单及 Origin 校验，禁用公共资料变更入口。
3. 可信会话守卫及初始化 CLI：安全终端隐藏密码，事务一次创建，不认领旧账户、不重设密码。
4. 临时数据库验证登录、越权、过期、注册、Origin、初始化冲突、幂等；类型、lint、构建。

不执行真实数据库写入、不读取或修改 .env、不实现项目/归档/界面。
