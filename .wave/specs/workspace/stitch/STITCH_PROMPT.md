# workspace Stitch 生成提示词

## 元数据

- 模式：PROMPT_ONLY
- 来源 PRD：[INIT v3](../../../prd/INIT-PRD.md)，已确认
- 来源 MODULE：[MODULE](../MODULE.md)
- 来源 DESIGN：[DESIGN](../DESIGN.md)
- 来源 UI：[UI](../UI.md)
- 平台：Web
- 设备：DESKTOP；另作 MOBILE 布局变体
- 优先策略：desktop-first
- Stitch 项目：未创建 / 未指定；本轮无外部调用
- 设计系统：PROPOSED；无项目 ID
- 状态：READY
- 生成时间：2026-09-15T16:31:37+08:00

## PRD → 屏幕映射

| 屏幕 / 路由 | 来源 F/AC | 用户任务 | 入口 | 成功结果 | 提示词状态 |
|---|---|---|---|---|---|
| 最近项目 / `/` | F-001/AC-001, F-010/AC-010 | 返回保存的项目或进入专业 / 快捷创作 | 根导航或登录后 | 用户可见主要动作、反馈与恢复入口 | READY |
| 拍摄项目 / `/projects` | F-001/AC-001, F-010/AC-010 | 查找、保存和再次打开私有项目 | 全局项目导航 | 用户可见主要动作、反馈与恢复入口 | READY |
| 项目详情 / `/projects/:projectId` | F-001/AC-001, F-010/AC-010 | 打开同一项目并继续拍摄草稿 | 项目卡片 | 用户可见主要动作、反馈与恢复入口 | READY |
| 归档与恢复 / `/archive` | F-010/AC-010, F-009/AC-009 | 找回归档对象并保留图片与历史关联 | 次级归档入口 | 用户可见主要动作、反馈与恢复入口 | READY |
| 私有登录 / `/sign-in` | F-001/AC-001, F-010/AC-010 | 进入个人私有工作区 | 未认证访问 | 用户可见主要动作、反馈与恢复入口 | READY |

## 共享约束

产品：praxis-vision，个人 AI 虚构人像创作者的私有工作台。界面用简体中文，项目名沿用已有名称；不新增品牌口号。
导航：项目、人物、摄影技能、摄影风格、作品；快捷生成常驻，归档次级入口。详情通过面包屑返回所属列表。
内容约束：只使用 AI 虚构人物；无真人照片上传、公开分享、永久删除、支付、团队、风光首版或模型训练。照片 / 人名 / 费用 / 测试结果使用清楚占位，不能伪造真实数据。归档始终解释为可恢复，不清除数据。设计 CTA 属于建议文案，不冒充最终批准。
设计系统：当前无 Stitch 项目设计系统，视觉方向为 PROPOSED。先在 Stitch 建立或选择一个统一系统，再按屏幕结构生成；若已有系统，继承它，不在单屏提示词重复颜色 / 字体 / 圆角 token。
响应式：Web，desktop-first，DESKTOP；建议桌面画布 1440px、窄屏变体 390px（画布宽度是排版假设）。1024px 以下参数栏分区展开；768px 以下导航抽屉、单列。窄屏优先保证作品和任务查看，复杂编辑纵向展开，不承诺新增完整移动产品范围。
可访问性：正文 16px / 1.5，独立标签与错误摘要，可见焦点、语义标题、图片替代说明、键盘可达、触控目标至少 44px、文字对比至少 4.5:1。状态不只依赖颜色，不强制 hover 或拖拽；减少动画。

## 统一设计系统提议（单独配置，不重复到每屏）

提议沿用现有 UI 方向：图片为中心的安静深色摄影工作台。背景 #18181B，面板 #27272A，主要文字 #FAFAFA，次要文字 #D4D4D8，浅色主按钮深色文字。系统中文无衬线回退，不加载外部字体，8px 间距基准。共享导航与参数面板宽度按现有 UI；阴影、圆角与未定义 token 留给统一系统，不编造品牌规范。此提议不是效果测试或用户品牌批准。

## 屏幕提示词：最近项目

### 来源

- F/AC：F-001/AC-001, F-010/AC-010
- UI 页面：`/`；[现有 UI](../UI.md) 与 [体验流程](../DESIGN.md)

### 提交给 Stitch 的生成提示词

```text
设计“最近项目”页面，帮助用户返回保存的项目或进入专业 / 快捷创作。路由：/；入口：根导航或登录后。

PLATFORM: Web，desktop-first，device DESKTOP；为 390px 窄屏另作布局变体，不把所有状态塞进一个画布。

PAGE STRUCTURE:
1. 页头：最近项目标题；主要入口“新建项目”、次要常驻入口“快捷生成”。
2. 项目卡片列表：项目名称、标签、保存时间；使用 [项目名称] 等占位内容，不编造统计图或收入卡。
3. 新建项目模态框：项目名称（1–80 字）和标签（最多 20 个），动作“创建项目”“取消”。

INTERACTIONS:
打开卡片进入 /projects/:projectId；创建成功进入项目并可选择专业创作；快捷入口进入 /quick。空态解释先创建项目或快捷生成。

KEY STATES:
default：展示本页结构、主要动作和清楚占位。loading：局部骨架或进行中文字，保留导航与已加载内容。empty：区分无资产 / 无筛选匹配；表单页无资产列表时此状态不适用，改用未填写初始态。error：保留输入、明确失败原因与相应重试入口。success：显示真实操作结果和已保存状态，概念图仅用 [保存结果] 占位。disabled：重复提交或能力未配置时禁用并说明原因。validation：有表单时字段旁错误加可聚焦摘要；无表单列表页为不适用。unauthorized/not found：不泄漏他人资源，返回本人列表或登录。interruption：区分草稿和已提交快照，重开核实状态。
初始画布聚焦 default；loading / empty / error / validation / disabled / success 分别作为可点选的状态变体。不得用假照片、费用或验证记录伪装业务运行结果。

RESPONSIVE AND ACCESSIBILITY:
1024px 以下参数栏变分区，768px 以下导航抽屉、单列。图片不导致页面横向溢出。键盘可操作，独立输入标签、错误摘要、焦点可见；触控目标 ≥44px，文本对比 ≥4.5:1。信息不只依赖颜色，图像有替代说明，对比支持按钮切换。

CONTENT CONSTRAINTS:
只使用 AI 虚构人物；无真人照片上传、公开分享、永久删除、支付、团队、风光首版或模型训练。照片 / 人名 / 费用 / 测试结果使用清楚占位，不能伪造真实数据。归档始终解释为可恢复，不清除数据。设计 CTA 属于建议文案，不冒充最终批准。
继承统一 Stitch 设计系统。本屏只规定结构、内容、层级与交互，不覆盖其 theme token。CTA 为建议文案，未知数据使用方括号占位；不增加 PRD 以外流程。
```

### 生成后定点编辑提示词

1. 在本页主内容区，只调整主要动作的层级，使其先于次级动作被看到；保持当前字段、导航与业务规则。
2. 在本页窄屏变体，只调整横向分栏为纵向分区，确保没有横向溢出并保留动作标签。
3. 在本页错误状态，只加入保留当前输入的局部恢复入口，不把重试保存或状态核实替换为再次生成。

## 屏幕提示词：拍摄项目

### 来源

- F/AC：F-001/AC-001, F-010/AC-010
- UI 页面：`/projects`；[现有 UI](../UI.md) 与 [体验流程](../DESIGN.md)

### 提交给 Stitch 的生成提示词

```text
设计“拍摄项目”页面，帮助用户查找、保存和再次打开私有项目。路由：/projects；入口：全局项目导航。

PLATFORM: Web，desktop-first，device DESKTOP；为 390px 窄屏另作布局变体，不把所有状态塞进一个画布。

PAGE STRUCTURE:
1. 标题栏：拍摄项目、新建项目。
2. 搜索框与项目卡片列表：名称、标签、保存状态，分页 / 加载更多入口。
3. 每个项目提供打开、编辑名称 / 标签、归档；归档提示可恢复且保留历史关联。

INTERACTIONS:
检索仅作用于当前工作区；筛选空态可清除查询；归档后从默认列表隐藏并可在 /archive 找回。

KEY STATES:
default：展示本页结构、主要动作和清楚占位。loading：局部骨架或进行中文字，保留导航与已加载内容。empty：区分无资产 / 无筛选匹配；表单页无资产列表时此状态不适用，改用未填写初始态。error：保留输入、明确失败原因与相应重试入口。success：显示真实操作结果和已保存状态，概念图仅用 [保存结果] 占位。disabled：重复提交或能力未配置时禁用并说明原因。validation：有表单时字段旁错误加可聚焦摘要；无表单列表页为不适用。unauthorized/not found：不泄漏他人资源，返回本人列表或登录。interruption：区分草稿和已提交快照，重开核实状态。
初始画布聚焦 default；loading / empty / error / validation / disabled / success 分别作为可点选的状态变体。不得用假照片、费用或验证记录伪装业务运行结果。

RESPONSIVE AND ACCESSIBILITY:
1024px 以下参数栏变分区，768px 以下导航抽屉、单列。图片不导致页面横向溢出。键盘可操作，独立输入标签、错误摘要、焦点可见；触控目标 ≥44px，文本对比 ≥4.5:1。信息不只依赖颜色，图像有替代说明，对比支持按钮切换。

CONTENT CONSTRAINTS:
只使用 AI 虚构人物；无真人照片上传、公开分享、永久删除、支付、团队、风光首版或模型训练。照片 / 人名 / 费用 / 测试结果使用清楚占位，不能伪造真实数据。归档始终解释为可恢复，不清除数据。设计 CTA 属于建议文案，不冒充最终批准。
继承统一 Stitch 设计系统。本屏只规定结构、内容、层级与交互，不覆盖其 theme token。CTA 为建议文案，未知数据使用方括号占位；不增加 PRD 以外流程。
```

### 生成后定点编辑提示词

1. 在本页主内容区，只调整主要动作的层级，使其先于次级动作被看到；保持当前字段、导航与业务规则。
2. 在本页窄屏变体，只调整横向分栏为纵向分区，确保没有横向溢出并保留动作标签。
3. 在本页错误状态，只加入保留当前输入的局部恢复入口，不把重试保存或状态核实替换为再次生成。

## 屏幕提示词：项目详情

### 来源

- F/AC：F-001/AC-001, F-010/AC-010
- UI 页面：`/projects/:projectId`；[现有 UI](../UI.md) 与 [体验流程](../DESIGN.md)

### 提交给 Stitch 的生成提示词

```text
设计“项目详情”页面，帮助用户打开同一项目并继续拍摄草稿。路由：/projects/:projectId；入口：项目卡片。

PLATFORM: Web，desktop-first，device DESKTOP；为 390px 窄屏另作布局变体，不把所有状态塞进一个画布。

PAGE STRUCTURE:
1. 面包屑“项目 / [项目名称]”；名称和标签编辑区，明确保存中 / 已保存 / 保存失败。
2. 专业创作入口，指向 /studio/:projectId；快捷创作入口指向 /quick，项目来源仅在实际关联时显示。
3. 本项目已保存方案与作品区域：来源版本、草稿标识、打开动作；没有作品时显示空态。

INTERACTIONS:
返回草稿保留已保存设置；离开未保存内容时提供保存 / 放弃 / 继续编辑。归档可恢复，不能显示为数据删除。

KEY STATES:
default：展示本页结构、主要动作和清楚占位。loading：局部骨架或进行中文字，保留导航与已加载内容。empty：区分无资产 / 无筛选匹配；表单页无资产列表时此状态不适用，改用未填写初始态。error：保留输入、明确失败原因与相应重试入口。success：显示真实操作结果和已保存状态，概念图仅用 [保存结果] 占位。disabled：重复提交或能力未配置时禁用并说明原因。validation：有表单时字段旁错误加可聚焦摘要；无表单列表页为不适用。unauthorized/not found：不泄漏他人资源，返回本人列表或登录。interruption：区分草稿和已提交快照，重开核实状态。
初始画布聚焦 default；loading / empty / error / validation / disabled / success 分别作为可点选的状态变体。不得用假照片、费用或验证记录伪装业务运行结果。

RESPONSIVE AND ACCESSIBILITY:
1024px 以下参数栏变分区，768px 以下导航抽屉、单列。图片不导致页面横向溢出。键盘可操作，独立输入标签、错误摘要、焦点可见；触控目标 ≥44px，文本对比 ≥4.5:1。信息不只依赖颜色，图像有替代说明，对比支持按钮切换。

CONTENT CONSTRAINTS:
只使用 AI 虚构人物；无真人照片上传、公开分享、永久删除、支付、团队、风光首版或模型训练。照片 / 人名 / 费用 / 测试结果使用清楚占位，不能伪造真实数据。归档始终解释为可恢复，不清除数据。设计 CTA 属于建议文案，不冒充最终批准。
继承统一 Stitch 设计系统。本屏只规定结构、内容、层级与交互，不覆盖其 theme token。CTA 为建议文案，未知数据使用方括号占位；不增加 PRD 以外流程。
```

### 生成后定点编辑提示词

1. 在本页主内容区，只调整主要动作的层级，使其先于次级动作被看到；保持当前字段、导航与业务规则。
2. 在本页窄屏变体，只调整横向分栏为纵向分区，确保没有横向溢出并保留动作标签。
3. 在本页错误状态，只加入保留当前输入的局部恢复入口，不把重试保存或状态核实替换为再次生成。

## 屏幕提示词：归档与恢复

### 来源

- F/AC：F-010/AC-010, F-009/AC-009
- UI 页面：`/archive`；[现有 UI](../UI.md) 与 [体验流程](../DESIGN.md)

### 提交给 Stitch 的生成提示词

```text
设计“归档与恢复”页面，帮助用户找回归档对象并保留图片与历史关联。路由：/archive；入口：次级归档入口。

PLATFORM: Web，desktop-first，device DESKTOP；为 390px 窄屏另作布局变体，不把所有状态塞进一个画布。

PAGE STRUCTURE:
1. 标题“归档与恢复”，帮助文字“可恢复，素材和历史关联仍保留”。
2. 类别筛选：项目、人物、风格、技能、作品；列表显示对象名称、类别和归档标识。
3. 每项主要动作“恢复”，成功反馈“已恢复”；提供返回所属列表。

INTERACTIONS:
恢复失败保留条目和重试恢复入口；归档作品仍可查看历史快照；没有永久删除、清空归档或数据清除动作。

KEY STATES:
default：展示本页结构、主要动作和清楚占位。loading：局部骨架或进行中文字，保留导航与已加载内容。empty：区分无资产 / 无筛选匹配；表单页无资产列表时此状态不适用，改用未填写初始态。error：保留输入、明确失败原因与相应重试入口。success：显示真实操作结果和已保存状态，概念图仅用 [保存结果] 占位。disabled：重复提交或能力未配置时禁用并说明原因。validation：有表单时字段旁错误加可聚焦摘要；无表单列表页为不适用。unauthorized/not found：不泄漏他人资源，返回本人列表或登录。interruption：区分草稿和已提交快照，重开核实状态。
初始画布聚焦 default；loading / empty / error / validation / disabled / success 分别作为可点选的状态变体。不得用假照片、费用或验证记录伪装业务运行结果。

RESPONSIVE AND ACCESSIBILITY:
1024px 以下参数栏变分区，768px 以下导航抽屉、单列。图片不导致页面横向溢出。键盘可操作，独立输入标签、错误摘要、焦点可见；触控目标 ≥44px，文本对比 ≥4.5:1。信息不只依赖颜色，图像有替代说明，对比支持按钮切换。

CONTENT CONSTRAINTS:
只使用 AI 虚构人物；无真人照片上传、公开分享、永久删除、支付、团队、风光首版或模型训练。照片 / 人名 / 费用 / 测试结果使用清楚占位，不能伪造真实数据。归档始终解释为可恢复，不清除数据。设计 CTA 属于建议文案，不冒充最终批准。
继承统一 Stitch 设计系统。本屏只规定结构、内容、层级与交互，不覆盖其 theme token。CTA 为建议文案，未知数据使用方括号占位；不增加 PRD 以外流程。
```

### 生成后定点编辑提示词

1. 在本页主内容区，只调整主要动作的层级，使其先于次级动作被看到；保持当前字段、导航与业务规则。
2. 在本页窄屏变体，只调整横向分栏为纵向分区，确保没有横向溢出并保留动作标签。
3. 在本页错误状态，只加入保留当前输入的局部恢复入口，不把重试保存或状态核实替换为再次生成。

## 屏幕提示词：私有登录

### 来源

- F/AC：F-001/AC-001, F-010/AC-010
- UI 页面：`/sign-in`；[现有 UI](../UI.md) 与 [体验流程](../DESIGN.md)

### 提交给 Stitch 的生成提示词

```text
设计“私有登录”页面，帮助用户进入个人私有工作区。路由：/sign-in；入口：未认证访问。

PLATFORM: Web，desktop-first，device DESKTOP；为 390px 窄屏另作布局变体，不把所有状态塞进一个画布。

PAGE STRUCTURE:
1. 居中紧凑会话面板，标题“登录个人工作区”；所有者邮箱与密码有独立标签。
2. 主按钮“登录”；字段错误和表单错误摘要，提交中状态。
3. 说明仅预配置所有者访问；无注册、邀请、公开主页或分享入口。

INTERACTIONS:
登录成功返回站内原任务；会话失效提示重新登录；密码使用遮蔽输入，不放凭据示例。

KEY STATES:
default：展示本页结构、主要动作和清楚占位。loading：局部骨架或进行中文字，保留导航与已加载内容。empty：区分无资产 / 无筛选匹配；表单页无资产列表时此状态不适用，改用未填写初始态。error：保留输入、明确失败原因与相应重试入口。success：显示真实操作结果和已保存状态，概念图仅用 [保存结果] 占位。disabled：重复提交或能力未配置时禁用并说明原因。validation：有表单时字段旁错误加可聚焦摘要；无表单列表页为不适用。unauthorized/not found：不泄漏他人资源，返回本人列表或登录。interruption：区分草稿和已提交快照，重开核实状态。
初始画布聚焦 default；loading / empty / error / validation / disabled / success 分别作为可点选的状态变体。不得用假照片、费用或验证记录伪装业务运行结果。

RESPONSIVE AND ACCESSIBILITY:
1024px 以下参数栏变分区，768px 以下导航抽屉、单列。图片不导致页面横向溢出。键盘可操作，独立输入标签、错误摘要、焦点可见；触控目标 ≥44px，文本对比 ≥4.5:1。信息不只依赖颜色，图像有替代说明，对比支持按钮切换。

CONTENT CONSTRAINTS:
只使用 AI 虚构人物；无真人照片上传、公开分享、永久删除、支付、团队、风光首版或模型训练。照片 / 人名 / 费用 / 测试结果使用清楚占位，不能伪造真实数据。归档始终解释为可恢复，不清除数据。设计 CTA 属于建议文案，不冒充最终批准。
继承统一 Stitch 设计系统。本屏只规定结构、内容、层级与交互，不覆盖其 theme token。CTA 为建议文案，未知数据使用方括号占位；不增加 PRD 以外流程。
```

### 生成后定点编辑提示词

1. 在本页主内容区，只调整主要动作的层级，使其先于次级动作被看到；保持当前字段、导航与业务规则。
2. 在本页窄屏变体，只调整横向分栏为纵向分区，确保没有横向溢出并保留动作标签。
3. 在本页错误状态，只加入保留当前输入的局部恢复入口，不把重试保存或状态核实替换为再次生成。

## 假设与待确认

- 深色视觉方向、画布宽度和 CTA 文案是现有规划的排版 / 文案假设，未做可用性验证；无品牌资源和外部图片。
- 尚无 Stitch 项目或统一设计系统；正式生成时先配置系统。当前仅本地提示词，不上传资料或创建外部设计稿。
- 页内状态不要求同一画布同时展示；图片采用标注占位，不假称生成结果或已验证方法。

## 生成前检查

- [x] 每屏回链 F/AC 和现有 UI 页面
- [x] 平台、设备和优先策略明确
- [x] 页面结构、动作与关键状态完整；不适用项有说明
- [x] 未编造品牌、真实业务数据或新业务规则
- [x] 视觉系统独立提议，单屏结构提示词不重复 token
- [x] 每个屏幕内容可独立复制；先配置共享系统
- [ ] 外部生成 / 屏幕 ID / 视觉评审：本轮未执行
