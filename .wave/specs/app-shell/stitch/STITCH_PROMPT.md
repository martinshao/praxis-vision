# app-shell Stitch 生成提示词

## 元数据

- 模式：PROMPT_ONLY
- 来源 PRD：[INIT v3](../../../prd/INIT-PRD.md)，已确认
- 来源 MODULE：[MODULE](../MODULE.md)
- 来源 DESIGN：[DESIGN](../DESIGN.md)
- 来源 UI：[UI](../UI.md)
- 平台：Web
- 设备：DESKTOP；另作 MOBILE 布局变体
- 优先策略：desktop-first
- Stitch 项目：未创建 / 未指定；本轮无外部调用
- 设计系统：PROPOSED；无项目 ID
- 状态：READY
- 生成时间：2026-09-15T16:31:37+08:00

## PRD → 屏幕映射

| 屏幕 / 路由 | 来源 F/AC | 用户任务 | 入口 | 成功结果 | 提示词状态 |
|---|---|---|---|---|---|
| 全局应用壳与占位规范 / `全部 18 规划路由` | F-001/AC-001, F-010/AC-010 | 在一致导航中访问项目、人物、技能、风格、作品和快捷入口 | 任意业务页面 | 用户可见主要动作、反馈与恢复入口 | READY |

## 共享约束

产品：praxis-vision，个人 AI 虚构人像创作者的私有工作台。界面用简体中文，项目名沿用已有名称；不新增品牌口号。
导航：项目、人物、摄影技能、摄影风格、作品；快捷生成常驻，归档次级入口。详情通过面包屑返回所属列表。
内容约束：只使用 AI 虚构人物；无真人照片上传、公开分享、永久删除、支付、团队、风光首版或模型训练。照片 / 人名 / 费用 / 测试结果使用清楚占位，不能伪造真实数据。归档始终解释为可恢复，不清除数据。设计 CTA 属于建议文案，不冒充最终批准。
设计系统：当前无 Stitch 项目设计系统，视觉方向为 PROPOSED。先在 Stitch 建立或选择一个统一系统，再按屏幕结构生成；若已有系统，继承它，不在单屏提示词重复颜色 / 字体 / 圆角 token。
响应式：Web，desktop-first，DESKTOP；建议桌面画布 1440px、窄屏变体 390px（画布宽度是排版假设）。1024px 以下参数栏分区展开；768px 以下导航抽屉、单列。窄屏优先保证作品和任务查看，复杂编辑纵向展开，不承诺新增完整移动产品范围。
可访问性：正文 16px / 1.5，独立标签与错误摘要，可见焦点、语义标题、图片替代说明、键盘可达、触控目标至少 44px、文字对比至少 4.5:1。状态不只依赖颜色，不强制 hover 或拖拽；减少动画。

## 统一设计系统提议（单独配置，不重复到每屏）

提议沿用现有 UI 方向：图片为中心的安静深色摄影工作台。背景 #18181B，面板 #27272A，主要文字 #FAFAFA，次要文字 #D4D4D8，浅色主按钮深色文字。系统中文无衬线回退，不加载外部字体，8px 间距基准。共享导航与参数面板宽度按现有 UI；阴影、圆角与未定义 token 留给统一系统，不编造品牌规范。此提议不是效果测试或用户品牌批准。

## 屏幕提示词：全局应用壳与占位规范

### 来源

- F/AC：F-001/AC-001, F-010/AC-010
- UI 页面：`全部 18 规划路由`；[现有 UI](../UI.md) 与 [体验流程](../DESIGN.md)

### 提交给 Stitch 的生成提示词

```text
设计“全局应用壳与占位规范”页面，帮助用户在一致导航中访问项目、人物、技能、风格、作品和快捷入口。路由：全部 18 规划路由；入口：任意业务页面。

PLATFORM: Web，desktop-first，device DESKTOP；为 390px 窄屏另作布局变体，不把所有状态塞进一个画布。

PAGE STRUCTURE:
1. 桌面左侧约 224px 根导航：项目、人物、摄影技能、摄影风格、作品；快捷生成常驻，归档次级入口。
2. 顶部栏与面包屑、当前页标题、说明、弹性内容容器；只展示占位区域和“功能尚未实现”。
3. 独立 404 / 错误边界变体：明确错误文字、返回项目或局部重试；登录页只占位。

INTERACTIONS:
当前路由可见标记，详情面包屑返回所属列表，工作台返回项目。抽屉焦点管理、Escape 关闭；壳层不放真实表单、业务结果、API 或生成逻辑。所有业务字段设计由对应模块提示词承接。

KEY STATES:
default：占位页。loading：保留导航。empty：内容占位。error：错误边界 / 404 独立变体。success：导航抵达目标页，无业务成功结果。disabled：无业务按钮，不适用。validation：无业务表单，不适用。
初始画布聚焦 default；loading / empty / error / validation / disabled / success 分别作为可点选的状态变体。不得用假照片、费用或验证记录伪装业务运行结果。

RESPONSIVE AND ACCESSIBILITY:
1024px 以下参数栏变分区，768px 以下导航抽屉、单列。图片不导致页面横向溢出。键盘可操作，独立输入标签、错误摘要、焦点可见；触控目标 ≥44px，文本对比 ≥4.5:1。信息不只依赖颜色，图像有替代说明，对比支持按钮切换。

CONTENT CONSTRAINTS:
只使用 AI 虚构人物；无真人照片上传、公开分享、永久删除、支付、团队、风光首版或模型训练。照片 / 人名 / 费用 / 测试结果使用清楚占位，不能伪造真实数据。归档始终解释为可恢复，不清除数据。设计 CTA 属于建议文案，不冒充最终批准。
继承统一 Stitch 设计系统。本屏只规定结构、内容、层级与交互，不覆盖其 theme token。CTA 为建议文案，未知数据使用方括号占位；不增加 PRD 以外流程。
```

### 生成后定点编辑提示词

1. 在本页主内容区，只调整主要动作的层级，使其先于次级动作被看到；保持当前字段、导航与业务规则。
2. 在本页窄屏变体，只调整横向分栏为纵向分区，确保没有横向溢出并保留动作标签。
3. 在本页错误状态，只加入保留当前输入的局部恢复入口，不把重试保存或状态核实替换为再次生成。

## 假设与待确认

- 深色视觉方向、画布宽度和 CTA 文案是现有规划的排版 / 文案假设，未做可用性验证；无品牌资源和外部图片。
- 尚无 Stitch 项目或统一设计系统；正式生成时先配置系统。当前仅本地提示词，不上传资料或创建外部设计稿。
- 页内状态不要求同一画布同时展示；图片采用标注占位，不假称生成结果或已验证方法。

## 生成前检查

- [x] 每屏回链 F/AC 和现有 UI 页面
- [x] 平台、设备和优先策略明确
- [x] 页面结构、动作与关键状态完整；不适用项有说明
- [x] 未编造品牌、真实业务数据或新业务规则
- [x] 视觉系统独立提议，单屏结构提示词不重复 token
- [x] 每个屏幕内容可独立复制；先配置共享系统
- [ ] 外部生成 / 屏幕 ID / 视觉评审：本轮未执行
